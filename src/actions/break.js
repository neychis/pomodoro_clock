const DECREMENT = "DECREMENT_BREAK";
const INCREMENT = "INCREMENT_BREAK";
const START = "START_BREAK";

const breakActions = {
  INCREMENT,
  DECREMENT,
  START
};

export default breakActions;
