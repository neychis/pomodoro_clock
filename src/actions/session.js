const DECREMENT = "DECREMENT_SESSION";
const INCREMENT = "INCREMENT_SESSION";
const START = "START_SESSION";

const sessionActions = {
  INCREMENT,
  DECREMENT,
  START
};

export default sessionActions;
